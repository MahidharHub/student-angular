export interface Student {
    id?: string,
    firstName?: string,
    lastName?: string,
    city?: string,
    postcode?: string,
    enrolledDate?: any,
    batch?: string,
    enrolledCourse?: string
  }